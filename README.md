# UI
```
=======================================
*                                     *
*           Common Shell Tools        *
*                                     *
=======================================
Version:1.0.0

Welcome to use njq batch shell
spring-shell>open-account --sleep 3 --from 3 --to 10

注意:在IDE中运行会出现阻塞情况,打包后在命令行可以顺利执行。
java -jar xxx.jar

在启动脚本中指定classpath: ../config/就可以读取config目录中的配置
```
